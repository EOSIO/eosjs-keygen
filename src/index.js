
const generate = require('./generate')

module.exports = {
  generate
}

/**
  @typedef {string} masterPrivateKey
  @typedef {string} owner (wallet import format)
  @typedef {string} active (wallet import format)
*/
